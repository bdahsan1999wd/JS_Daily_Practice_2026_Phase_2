# 🌙 JS DAILY PRACTICE – DAY-92

📅 **Goal:** Health-Tech Data Visualization & API Logic
🎯 **Focus:** Data Mapping • Statistics Calculation • String Manipulation • Error Handling

---

## ⚠️ General Rules

- Solve every problem using a **function**.
- **Return** the result (❌ do not use `console.log` inside the function).
- Proper **input validation** is mandatory (type and range checks).
- If input is invalid → return `"Invalid Input"`.

---

## 🧩 PROBLEM–01: 📊 BMI Category Mapper

⚠️ **Function Name:** `getBMIDetails()`

A digital scale sends height and weight; you need to return a detailed health status object.

| Input      | `weightKg` (number), `heightCm` (number). |
| :--------- | :---------------------------------------- |
| **Output** | Return Object.                            |

**Rules:**

- **Validation:** Both must be positive numbers.
- **Logic:** $BMI = weight / (heightInMeters^2)$.
- **Categories:** - < 18.5 ➔ `"Underweight"`
  - 18.5 - 24.9 ➔ `"Normal"`
  - 25.0 - 29.9 ➔ `"Overweight"`
  - \>= 30.0 ➔ `"Obese"`

| Challenge 📢 | The return object must include `bmi` (rounded to 1 decimal), `category`, and a `suggestion` (e.g., "Maintain current lifestyle" for Normal, "Consult a nutritionist" for others). |
| :----------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |

**Sample Input & Output:**

- `getBMIDetails(70, 175)` ➔ `{ bmi: 22.9, category: "Normal", suggestion: "Maintain current lifestyle" }`

---

## 🧩 PROBLEM–02: 🕒 Multi-Zone Appointment Fixer

⚠️ **Function Name:** `convertAppointmentTime()`

Telemedicine allows international calls. Convert a Bangladesh (BST) time to a Target zone.

| Input      | `timeBST` (number 0-23), `zone` (string "GMT" or "EST"). |
| :--------- | :------------------------------------------------------- |
| **Output** | Return number (24-hour format).                          |

**Rules:**

- **Validation:** `timeBST` 0-23. `zone` must be "GMT" or "EST" (case-insensitive).
- **Logic:** - BST to GMT: Subtract 6 hours.
  - BST to EST: Subtract 11 hours.

| Challenge 📢 | Handle "Circular Time". If the subtraction results in a negative number, wrap it around (e.g., if it's 2:00 BST, GMT is 20:00 the previous day). |
| :----------- | :----------------------------------------------------------------------------------------------------------------------------------------------- |

**Sample Input & Output:**

- `convertAppointmentTime(2, "GMT")` ➔ `20`
- `convertAppointmentTime(10, "EST")` ➔ `23`

---

## 🧩 PROBLEM–03: 🔍 Patient Search Filter

⚠️ **Function Name:** `filterPatientsByRisk()`

Filter a list of patients based on their heart rate status.

| Input      | `patients` (Array of objects: `{name, hr}`), `level` (string). |
| :--------- | :------------------------------------------------------------- |
| **Output** | Return Array of strings (Names only).                          |

**Rules:**

- **Validation:** `patients` must be an array; `level` must be "High" or "Normal".
- **Logic:** - "High" ➔ Heart Rate (hr) > 100.
  - "Normal" ➔ Heart Rate (hr) between 60 and 100.

| Challenge 📢 | If the `level` is "High" and the resulting array is empty, return the string `"ALL_PATIENTS_STABLE"`. |
| :----------- | :---------------------------------------------------------------------------------------------------- |

**Sample Input & Output:**

- `filterPatientsByRisk([{name: "Abir", hr: 110}, {name: "Sumi", hr: 75}], "High")` ➔ `["Abir"]`

---

## 🧩 PROBLEM–04: 📑 Prescription Key Extractor

⚠️ **Function Name:** `getMedicineSummary()`

Extract specific details from a complex string generated by a legacy medical system.

| Input      | `rawString` (string format: "Med:Napa | Dose:500mg | Freq:3"). |
| :--------- | :------------------------------------ | ---------- | --------- |
| **Output** | Return Object.                        |

**Rules:**

- **Validation:** String must contain "Med:", "Dose:", and "Freq:".
- **Logic:** Parse the string and return an object `{ medicine, dosage, frequency }`.

| Challenge 📢 | Ensure all returned values are trimmed of extra whitespace and the `frequency` is converted to a `Number`. |
| :----------- | :--------------------------------------------------------------------------------------------------------- |

**Sample Input & Output:**

- `getMedicineSummary("Med: Ace | Dose: 500mg | Freq: 2")` ➔ `{ medicine: "Ace", dosage: "500mg", frequency: 2 }`

---

## 🧩 PROBLEM–05: 📈 Glucose Level Spike Detector

⚠️ **Function Name:** `detectGlucoseSpike()`

Detect if there was a sudden "spike" (increase of 40% or more) between two consecutive readings.

| Input      | `readings` (Array of numbers). |
| :--------- | :----------------------------- |
| **Output** | Return boolean.                |

**Rules:**

- **Validation:** Array must contain at least 2 numbers.
- **Logic:** Compare each reading with the next one. If any $reading_{i+1} \ge reading_i * 1.4$, return `true`.

| Challenge 📢 | If a spike is detected, instead of just `true`, return the string `"SPIKE_DETECTED_AT_INDEX_[i]"` where [i] is the index of the **first** reading of that pair. |
| :----------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------- |

**Sample Input & Output:**

- `detectGlucoseSpike([100, 110, 160, 120])` ➔ `"SPIKE_DETECTED_AT_INDEX_1"` (110 to 160 is > 40% increase).
